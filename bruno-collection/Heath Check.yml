info:
  name: Heath Check
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_ENDPOINT}}/health-check"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
