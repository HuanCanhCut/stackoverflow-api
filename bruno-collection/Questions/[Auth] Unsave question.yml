info:
  name: "[Auth] Unsave question"
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{API_ENDPOINT}}/questions/1/save"
  headers:
    - name: Authorization
      value: Bearer {{ACCESS_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
