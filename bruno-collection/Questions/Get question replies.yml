info:
  name: Get question replies
  type: http
  seq: 6

http:
  method: GET
  url: "{{API_ENDPOINT}}/questions/1/replies?order_by=newest&page=1&per_page=10"
  params:
    - name: order_by
      value: newest
      type: query
    - name: page
      value: "1"
      type: query
    - name: per_page
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
