info:
  name: Send forgot password code
  type: http
  seq: 5

http:
  method: POST
  url: "{{API_ENDPOINT}}/auth/forgot-password/code"
  body:
    type: form-urlencoded
    data:
      - name: email
        value: tronghuandev@gmail.com
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
