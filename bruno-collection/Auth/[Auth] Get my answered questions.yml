info:
  name: "[Auth] Get my answered questions"
  type: http
  seq: 10

http:
  method: GET
  url: "{{API_ENDPOINT}}/auth/me/answered-questions?page=1&per_page=10"
  params:
    - name: page
      value: "1"
      type: query
    - name: per_page
      value: "10"
      type: query
  headers:
    - name: Authorization
      value: Bearer {{ACCESS_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
