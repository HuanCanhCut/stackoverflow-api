info:
  name: Reset password
  type: http
  seq: 6

http:
  method: POST
  url: "{{API_ENDPOINT}}/auth/reset-password"
  body:
    type: form-urlencoded
    data:
      - name: email
        value: tronghuandev@gmail.com
      - name: code
        value: "123456"
      - name: password
        value: "12345678"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
